extends layout

block content 
    main.container 
        h1= title 
        a.back-button(href="/") ⬅ Volver
        form(action="/add" method="POST")
            label(for="title") Título de la tarea
            input(type="text" id="title" name="title", required)
            button.add-button(type="submit") Agregar
        